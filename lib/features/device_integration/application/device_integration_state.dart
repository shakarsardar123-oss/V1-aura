/// device_integration_state.dart
///
/// Application-layer re-export of the device-integration state model, so that
/// controllers and presentation code depend on the application layer rather than
/// reaching into `domain/models` directly.
///
/// RECOVERED in P1-RECOVERY step R2.
library;

export '../domain/models/device_integration_state.dart';
